# AI Coach 推荐系统完整缺陷审计

## 已确认根因

### 1. DeepSeek 可以显式传入对手 `player_id`

原来的工具层只在模型没有给出 `player_id` 时才补成人类玩家。模型若主动传入 `3`，推荐工具会分析黄方，随后其结果还能覆盖本地规则报告。

**修复：** 人机模式下，面向玩家的推荐、排序、评价和局面解释统一锁定为人类红方。DeepSeek 的同名参数会被覆盖，不能改变推荐视角。

### 2. 覆盖层使用了“当前回合玩家”的合法走法校验

本地教练可能在 AI 回合分析红方，但覆盖层却用实际 `current_player` 的合法走法验证。更严重的是，验证失败的候选仍会被加入前端数据，只是标记为 `verified=false`。

**修复：** 覆盖层使用教练目标玩家的独立状态进行验证；未通过任一不变量的候选直接丢弃，不再发送给前端。

### 3. 模型文字坐标可以自动生成棋盘标记

模型只要在文字中输出形如“（行,列）→（行,列）”，旧版就可能将其变成覆盖层，即使起点为空、属于对手或走法非法。

**修复：** 文字坐标仍可点击用于定位，但不能生成走法覆盖层。覆盖层只接受规则引擎生成并验证的结构化数据。

### 4. 首次显示只画目标，没有显示起点棋子

旧版收到候选后默认不选择任何一项，只显示空目标孔的编号。玩家很容易误以为系统建议操作该空孔或对手区域。

**修复：** 首选候选自动展开，立即显示起点棋子、每段路径、跳板和目标。

### 5. 同目标候选的编号被故意偏移

多个候选落到同一目标时，旧版把圆形编号从孔位中心向四周偏移。编号看起来像一个落在两个棋格中间的“推荐点”。

**修复：** 目标圆环永远绘制在真实孔位中心；只有编号徽标可以偏移，并通过引线明确连接到目标孔。

### 6. 分析期间棋局可能已经变化

DeepSeek 响应可能需要数秒。若期间 AI 已落子，返回的分析属于旧局面。

**修复：** 后端和前端共同校验 `move_count` 与 `state_token`。状态变化时，后端自动重建当前局面的本地可解释报告；前端也会拒绝过期覆盖层。

### 7. 模型文字可能与规则报告冲突

即使结构化覆盖层正确，模型文字仍可能描述另一个坐标。

**修复：** 对推荐类回答提取模型给出的走法并与规则报告候选比对。出现冲突时，自动回退到本地可验证报告。

## 当前推荐不变量

每一个可显示候选必须同时满足：

1. 起点和终点均为 121 个有效孔位之一；
2. 起点当前存在目标玩家的真实棋子；
3. 终点当前为空；
4. 走法属于该玩家当前局面的合法走法集合；
5. 完整路径由规则引擎重新构造，不能采用模型自报路径；
6. 候选玩家与 AI Coach 的分析玩家一致；
7. 候选生成时的 `move_count` 和 `state_token` 与前端当前局面一致。

## 审计范围

- 首回合，人类先手与 AI 先手；
- 2、3、4、6 人棋盘；
- 单步、单跳、多段连跳；
- DeepSeek 错误玩家参数、虚构文字坐标、状态竞争；
- 同目标多候选的绘制与点击；
- 高 DPI 坐标投影和孔位中心一致性；
- 本地 Coach、DeepSeek、MCP、SQL 和启动器回归。

## 随机状态性质检查

构建阶段额外检查了 3,200 个随机局面、392,927 条合法走法：

- 非法来源候选：0；
- 空起点候选：0；
- 路径无法重建：0；
- 规则验证失败：0。
