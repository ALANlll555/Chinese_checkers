# AI 对话、MCP 与 SQL 升级说明

## 执行范围

本次以 AI 决策升级版为基础，仅增加以下必要能力：

1. 左侧 DeepSeek 对话栏；
2. DeepSeek 工具调用适配器；
3. 独立 MCP 服务；
4. SQLite 存储与审计；
5. 对应测试和文档。

未修改棋盘规则、棋子数量、三档 AI 算法、Canvas 尺寸、主题体系、音频系统和右侧控制栏功能。

## UI 约束

- 棋盘仍为 `720 × 680` Canvas。
- 右侧栏保留原结构和交互。
- 左侧栏复用 `.panel` 的 240px 宽度、主题变量、卡片、按钮和圆角。
- 桌面布局由“棋盘 + 右栏”扩展为“左栏 + 棋盘 + 右栏”，不改变棋盘内部布局。

## DeepSeek 对话

- 每条回答均可调用当前棋局工具。
- 工具层只读，不替玩家执行走棋。
- 对话历史写入 SQL。
- 无 Key、网络异常或 LLM 异常时，棋局继续正常运行。
- Key 不持久化。

## MCP

MCP 与浏览器对话共用 `game_tools.py`，避免出现两套评价或合法性逻辑。MCP 从 SQL 读取最新持久化状态，因此可独立于 Flask 进程运行。

接口覆盖：

- 棋局列表、元数据、完整状态；
- 走棋历史、AI 决策、评价快照、对话历史、工具审计和棋谱；
- 棋盘几何、合法走法；
- 五项评价、具体走法评价；
- 候选排序、三档 AI 推荐；
- 多步只读模拟、对局统计。

## SQL

使用 SQLite，记录：

- `games`
- `moves`
- `ai_decisions`
- `evaluation_snapshots`
- `chat_messages`
- `mcp_audit_logs`

服务器重启后，浏览器 Session 对应的棋局可从 SQL 恢复；DeepSeek Key 因安全原因需要重新输入或使用环境变量。

## 回归验证

测试覆盖：

- 原有 9 项 AI 测试；
- SQL 新建、落子、AI 决策、评价、悔棋、对话和审计；
- 工具合法性、评价、推荐和只读模拟；
- DeepSeek 工具调用循环与无 Key 降级；
- 左栏存在、左右等宽、棋盘尺寸不变；
- Flask 接口集成测试（测试环境具备 Flask 时执行）。


# 坐标可视化 / Markdown / UI 优化版

## 新增

- 可开关的 121 孔坐标层、鼠标坐标读取和聊天坐标点击定位。
- DeepSeek 结构化 `board_overlay`：推荐、候选、评价、模拟和最近一步可在主棋盘临时显示。
- 棋盘标记明确标注为只读；真实局面变化后自动清除。
- 安全 Markdown 渲染，解决标题前 `#`、粗体前后 `**`、列表前 `*` 直接显示的问题。
- DeepSeek 可选 Thinking 与 Strict Tools 环境配置，并补充棋盘坐标工具。
- 聊天气泡、表格、代码、焦点状态和窄屏布局细节优化。

## 保持不变

- 棋盘尺寸仍为 720 × 680。
- 棋盘规则、三档 AI、评分算法、SQL、MCP、棋谱、音频和全部原有按钮功能不变。
- AI 标记不会修改 `gameState`，不会提交 `/api/move`，不会代替用户落子。

# 高清坐标与候选路径交互版

## 改进

- 高 DPI Canvas：不改变 720 × 680 逻辑尺寸，提升高分屏坐标和棋子边缘清晰度。
- 坐标层新增“空位优先 / 全部孔位”切换；有棋子坐标采用旁置标签和引线，避免覆盖棋子。
- 候选目标先以 1–5 编号展示；点击目标后显示来源棋子、完整连跳落点序列、跳过的棋子和目标。
- DeepSeek/MCP 计算结果新增 `move_detail`、`candidate_details`、`path`、`jumped_over`、`jump_count` 和 `path_text`。
- 聊天区域新增候选卡片和全部路径折叠区，可直接驱动棋盘候选选择。
- 候选标记只在未选中真实棋子时接管点击；正常落子流程保持最高优先级。

## 保持不变

- 棋盘规则、三档 AI、五项评价、SQL、MCP、回放、音频、右侧控制和全部现有接口不变。


# 一键启动发布版

- 删除重复的 `启动跳棋.bat` 和 `启动MCP.bat`。
- 新增唯一入口 `启动游戏.bat`。
- 首次运行自动创建 `.venv` 并安装依赖；后续仅在依赖清单变化或组件缺失时更新。
- 游戏启动时自动在后台启动 MCP Streamable HTTP 服务。
- 浏览器自动打开；退出启动窗口时一并清理本次 MCP 子进程。
- Flask 发布启动关闭 Debug 与自动重载，避免重复进程。
- 游戏、MCP 默认只绑定本机回环地址。


# 启动器闪退修复

- 将 `启动游戏.bat` 改为无 BOM 的纯 ASCII 脚本。
- 新增根目录 `launcher.py`，负责完整解压检查、虚拟环境、日志和进程返回码。
- 启动窗口在成功停止或失败后都会等待用户确认。
- MCP HTTP 服务改由 `streamable_http_app()` 配合 Uvicorn 显式绑定端口。
- MCP 被降级为非阻塞可选服务：失败只记录警告，游戏仍启动。
- 依赖安装失败时保留详细提示，并将诊断写入 `startup.log`。


## Explainable AI Coach 与大棋盘恢复版

- AI Coach 默认从人类玩家视角分析，不再因当前轮到 AI 而误分析对手。
- 即使没有 DeepSeek Key、网络异常或 MCP 不可用，本地规则引擎仍会输出完整分析。
- 每份分析包含：局面阶段、五项特征、诊断、最多五个候选、完整路径、收益、代价、反事实和置信度。
- 这些内容是可核验的特征证据，不是语言模型私有思维链。
- 棋盘逻辑尺寸由 720×680 放大至 820×760，孔距与棋子同步放大；页面会响应式缩放并保留四周安全边距。


# 推荐与坐标覆盖层完整修复

- 修复 DeepSeek 工具参数可覆盖人类玩家视角的问题。
- 修复未验证文本坐标可能覆盖规则引擎建议的问题。
- 修复同目标候选编号偏移后被误认为目标点落在棋格之间的问题。
- AI Coach 推荐改为稳定、可审计排序；实际游戏 AI 的受控随机保持不变。
- 增加后端、前端双重校验与局面状态令牌。


# 中局空跳 / 长上下文 / 解释持久化

- 禁止从模型纯文本恢复走法覆盖层。
- 增加绘制时实时校验和所有回放状态变更清理。
- 增加同一棋子的连跳幽灵落点，消除“空点跳空点”的视觉歧义。
- SQL 聊天消息新增 metadata_json，持久化 Coach 报告和候选高亮。
- DeepSeek V4 采用 1M context 配置、900K 输入预算、4K 默认输出和 token-aware history trimming。
- 模型回复过短时自动补齐本地可验证解释。


# 推理过程 / 1M 上下文开关

- 恢复 DeepSeek `reasoning_content` 显示，采用默认折叠的独立面板。
- 推理内容与规则证据、棋盘覆盖层完全隔离。
- 增加 1M/标准上下文切换，并展示实际使用的上下文档位与预算。
- 思考模式不再发送 `tool_choice`、`temperature` 等不兼容或无效参数。
- 工具调用的 assistant 消息会携带原始 `reasoning_content` 进入下一子轮。
- 推理内容与上下文配置写入聊天元数据，刷新后可恢复。


# DeepSeek 开局设置与 MCP 调用可观测性

- 恢复开局前 Base URL、模型、思考模式、Strict Tools 等完整设置。
- 增加开局前 1M 上下文、推理显示、推理强度和最大输出设置。
- 增加无需创建对局的 DeepSeek 配置测试。
- 对话等待期间显示实时秒表。
- 推理面板显示 reasoning_content、字数和推理/生成耗时。
- MCP 工具面板显示参数、结果摘要、成功状态和毫秒耗时。
- 工具过程、耗时和设置写入 SQL 聊天元数据。


# 推理 / MCP 过程事件流

- 借鉴参考项目的单回复过程布局：思考块、工具轨迹、正文和耗时位于同一条 AI 回复内。
- 新增 `/api/chat/stream` SSE 接口；原 `/api/chat` 保留。
- 推理内容按模型轮次先于工具执行展示。
- MCP 共享工具在真实执行前显示“运行中”，完成后更新状态、参数、结果和耗时。
- 事件流异常不会改变走法、规则证据、SQL 持久化或本地降级结果。


# 全流程连续性与启动依赖优化

- 修复 MCP 工具结束后思考状态被提前终止的问题。
- 增加工具结果整理、二次推理、最终复核和回答组装阶段。
- SSE 每秒发送当前处理阶段心跳。
- 推理面板增加阶段时间线，总计时持续到最终回答出现。
- 启动器按模块与版本检查依赖，满足条件时完全跳过 pip。
- 可选依赖失败增加缓存退避和 `--repair` 手动重试。
- 新增首次启动缓存式后端全流程自检。


# DeepSeek 上游 Token 流式改造

- DeepSeek 上游请求改为 `stream: true`。
- 解析 `delta.reasoning_content`、`delta.content` 和增量 `tool_calls`。
- 工具名称和参数在模型生成过程中实时显示。
- 工具轮次中的完整 `reasoning_content` 在下一请求中原样回传。
- 工具 transcript 持久化，并在后续用户轮次恢复。
- 同步 `/api/chat` 兼容路径保持不变。
- 增加 `low / xhigh` 推理强度选项。

# 独立可收缩对话气泡

- 每一轮 `reasoning_content` 使用独立气泡。
- 工具、最终回答、Coach 报告和候选统一使用独立气泡。
- 单个气泡收缩后只显示标题。
- 新增 Token 不会改变用户的收缩状态。
- 工具参数草稿键加入模型轮次，解决不同轮次索引重用问题。
- 对重复事件、重复完成和重复错误显示增加幂等保护。


# AI 对话无输出热修复

- 修复实时 assistant turn 未挂载到消息列表的问题。
- 挂载发生在计时器、网络请求和 SSE 事件处理之前。
- 新增 DOM 挂载顺序回归测试。
- 使用真实 Chromium 验证多轮推理、MCP、最终回答和收缩行为。

# 默认气泡状态与聊天滚动

- 推理气泡和 MCP 气泡默认收缩。
- 最终回答、上下文、Coach 报告和候选默认展开。
- 删除流式事件中的无条件滚动到底部。
- 增加“接近底部”判定、用户滚动锁定和回到底部按钮。
- 增加触屏滚动与 overscroll 防护。
