# 全面 Bug 审计报告

## 复现到的真实问题

1. **历史文字重新生成未验证建议**  
   旧版在恢复聊天历史时，会从普通文本中的“坐标 → 坐标”重新构造候选。它没有当前局面的规则元数据，因此可能表现为空点到空点。

2. **部分状态变化没有统一清理覆盖层**  
   正常落子会清理建议，但进入回放、回放步进、退出回放和旧棋谱加载未全部走同一清理链，旧建议可能残留在新棋盘上。

3. **连跳视觉语义不清**  
   中间落点本来就是空孔。旧版把每段画成独立箭头和空心编号，容易被理解为“不同的不存在棋子从空点起跳”。

4. **解释结构没有持久化**  
   SQLite 只保存 AI 的纯文本。刷新或恢复会话后，Coach 报告、候选证据与高亮元数据丢失。

5. **上下文和输出预算偏小**  
   旧版只加载 24 条历史消息，默认输出上限 900 tokens，容易让 DeepSeek 的自然语言说明过短；虽然本地报告仍存在，但体验不稳定。

## 修复

- 普通模型文字只保留可点击坐标，**绝不再创建走法覆盖层**。
- 每次绘制、点击候选和恢复历史时，重新验证：
  - 局面令牌；
  - 步数；
  - 起点归属；
  - 终点为空；
  - 路径端点；
  - 有效孔位。
- 回放、重载和退出回放全部接入统一的覆盖层失效机制。
- 连跳改为“同一颗棋”的连续路径：每个中间落点显示半透明同色棋子和顺序编号。
- SQLite `chat_messages` 增加 `metadata_json`，保存并恢复：
  - `coach_report`
  - `board_overlay`
  - `coach_mode`
  - `coach_guard`
  - `context_usage`
- 历史报告若与当前棋局不匹配，只显示历史解释，不允许重新高亮当前棋盘。
- DeepSeek V4 默认配置：
  - 上下文窗口：1,000,000 tokens
  - 输入预算：900,000 tokens
  - 默认最大输出：4,096 tokens
  - 历史上限：500 条
  - 工具轮次：6
- 按近似 token 数裁剪历史，并保持稳定 system 前缀以利用 DeepSeek 自动上下文缓存。
- 无论 DeepSeek 文本质量如何，结构化本地 Coach 报告和交互候选始终独立返回。

## 验证范围

- 单元/契约测试：61 项
- 随机对局状态：2,880
- 检查合法走法：110,062
- 抽样 Coach 候选：2,880
- 数据库旧版本迁移：通过
- JavaScript 安全运行检查：通过
- 规则、实战 AI、启动器、MCP、回放、音频核心未修改

这些检查覆盖了已知推荐链、状态生命周期、SQL 恢复、前端绘制和长上下文路径；无法诚实保证任何软件“绝无所有未来 Bug”，但本次已把发现的根因变成了持续回归测试。
