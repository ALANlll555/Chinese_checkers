# 跳棋 · 对接文档（项目介绍 / 视频录制指引）

> **文档用途**：帮助后续人员快速了解本项目全貌，便于录制介绍视频、进行交接或二次开发。
> **配套文件**：`README.md`（精简版功能介绍）

---

## 一、项目一句话介绍

> **六角星棋盘跳棋** —— 一个基于 Flask 后端的 Web 对战平台，支持 2/3/4/6 人对战与人机对弈，提供 **6 套可切换皮肤主题**、**背景音乐** 与 **音效系统**，并内置 **三级 AI 引擎**、**对局回放**、**存档管理** 与 **LLM 智能点评**。

---

## 二、核心玩法

### 2.1 基本规则
- 棋盘为 **六角星形**，共 **121 个孔位**，每方 **10 颗棋子**
- 每方从自己的 **三角形起始区** 出发，目标是全部棋子进入 **对角的对方起始区**
- 走法两种：
  - **平移**：移动到相邻空位
  - **连跳**：隔一颗棋子（任意颜色）跳跃，可连续多跳

### 2.2 游戏模式
| 模式 | 说明 |
|------|------|
| **PVP 人人对战** | 支持 2 / 3 / 4 / 6 人轮流操作 |
| **PVE 人机对战** | 玩家与 AI 对战，AI 难度可选 |

### 2.3 玩家与棋盘方位
| 玩家编号 | 颜色 | 起始方位 |
|---------|------|---------|
| 0 | 红方 | 顶部 |
| 1 | 蓝方 | 右上 |
| 2 | 绿方 | 右下 |
| 3 | 黄方 | 底部 |
| 4 | 紫方 | 左下 |
| 5 | 橙方 | 左上 |

---

## 三、功能特性详解

### 3.1 主题皮肤系统（6 套）
面板顶部有 **6 个色块**，点击即时切换，**CSS 面板与 Canvas 棋盘同步变色**：

| 风格 | 浅色 | 深色 |
|------|------|------|
| iOS | 毛玻璃卡片 + SF 字体 + 系统蓝 | OLED 黑底 + 暗色毛玻璃 |
| 极简 | 纯白底 + 细灰边框 + 零阴影 | 纯黑底 + 高对比白字 |
| 卡通 | 奶油底 + 粗黑描边 + 漫画阴影 | 深紫底 + 霓虹品红发光 |

- 主题选择**自动保存**到浏览器，刷新不丢失
- 实现原理：CSS 自定义属性（CSS Variables）+ `data-theme` 属性，Canvas 通过 `getComputedStyle` 读取同一套变量

### 3.2 背景音乐（4 种风格，独立于主题）
- **钢琴**：稀疏单音旋律，安静克制（默认）
- **环境音**：柔和音垫 + 缓慢和声漂移
- **8-bit**：欢快芯片旋律，1.25 倍速（163 BPM）
- **Lo-fi**：爵士和弦进行（ii7-V7-Imaj7）+ 白噪声底噪
- **与视觉主题完全解耦**：例如 iOS 浅色皮肤 + 8-bit 音乐可自由组合
- BGM 默认关闭（规避浏览器自动播放限制），点击开启，音量可调

### 3.3 音效系统
全部使用 **Web Audio API 程序化合成**，零外部音频文件：

| 音效 | 触发时机 |
|------|----------|
| 选中 | 点击己方棋子 |
| 落子 | 完成一次走棋 |
| 连跳 | 每次跳跃 |
| 胜利 | 一方获胜（上行琶音） |
| 悔棋 | 撤销走棋 |
| 按钮 | 点击面板按钮 |

### 3.4 AI 引擎（三级难度）
| 难度 | 策略 | 说明 |
|------|------|------|
| 随机 | 随机走法 | 适合新手练习 |
| 贪心 | 局部最优 | 每步选择最大化评分的走法 |
| Minimax | Alpha-Beta 剪枝 | 深度搜索（默认深度 3），最强难度 |

### 3.5 AI 提示
- 使用 **最高难度 AI** 计算当前最佳走法并高亮显示，帮助玩家学习

### 3.6 悔棋
- PVP：撤销 1 步
- PVE：自动撤销 2 步（人类 + AI 各一步）

### 3.7 对局回放
- **对局中实时回放**：回顾已走过的每一步
- **存档回放**：加载历史存档逐步复盘
- 回放引擎从初始布局重建棋盘，逐步推进

### 3.8 存档管理
- **命名存档**：为对局起名保存，支持回放
- **快速开始**：不存档直接游玩
- **删除 / 重命名**：对已有存档操作
- 自动保存：每次走棋后后台线程自动保存

### 3.9 LLM 智能点评
- 可选接入 **DeepSeek API**（在设置面板输入 API Key）
- PVE 模式中 AI 走棋后，以 **电竞解说风格** 点评当前局面
- 不配置 API Key 不影响正常游戏

---

## 四、技术架构

### 4.1 前后端分离（模板 + REST API）
```
浏览器 (Canvas 前端)
   │  fetch (REST JSON)
   ▼
Flask 后端 (app.py)
   ├── board.py       规则引擎
   ├── ai.py          AI 引擎
   ├── replay.py      存档/棋谱
   ├── llm_comment.py DeepSeek 点评
   └── config.py      全局配置
```

### 4.2 关键技术点
- **后端**：Python Flask，无数据库（对局状态存内存 + 存档存 JSON 文件）
- **前端渲染**：原生 Canvas 2D 绘制棋盘/棋子/粒子，`requestAnimationFrame` 驱动动画
- **主题系统**：CSS 变量 + `data-theme` 切换，JS `Theme` 桥接对象读取变量
- **音频**：Web Audio API（`OscillatorNode` / `GainNode` / `BiquadFilter`）程序化合成
- **会话管理**：Flask `session` + 内存字典 `GAMES` 关联每个用户的对局

### 4.3 核心数据流
```
用户点击棋盘 → /api/legal_moves 获取合法走法 → 点击目标 →
  POST /api/move 校验并落子 → 返回新状态 → 前端重绘 →
    PVE 模式 → POST /api/ai_move AI 走棋 → /api/comment 获取点评
```

---

## 五、文件结构说明

```
跳棋/
├── app.py              # Flask Web 服务（页面 + 全部 REST API）
├── board.py            # 棋盘状态与规则引擎（BoardState 类）
├── ai.py               # AI 引擎（随机 / 贪心 / Minimax + 评估函数）
├── config.py           # 全局配置（棋盘坐标、玩家颜色、AI 参数）
├── replay.py           # 棋谱保存与加载
├── llm_comment.py      # LLM 点评（对接 DeepSeek API）
├── requirements.txt    # Python 依赖（flask, requests）
├── static/
│   ├── css/style.css   # 多主题 CSS 变量体系 + 6 套皮肤样式
│   ├── js/audio.js     # Web Audio 音效引擎 + 4 种 BGM 生成器
│   └── js/game.js      # Canvas 棋盘渲染 + 交互 + Theme 桥接 + 回放引擎
├── templates/
│   └── index.html      # 前端页面（棋盘 + 控制面板 + 弹窗）
└── replays/            # 存档目录（自动生成）
```

---

## 六、后端 API 速查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 首页 |
| POST | `/api/new_game` | 新建对局（模式/人数/难度/先手） |
| POST | `/api/move` | 人类走棋 |
| POST | `/api/ai_move` | AI 走棋 |
| POST | `/api/undo` | 悔棋 |
| POST | `/api/hint` | AI 提示 |
| GET | `/api/comment` | LLM 点评 |
| GET | `/api/state` | 获取当前局面 |
| GET | `/api/legal_moves?r=&c=` | 指定棋子的合法走法 |
| GET | `/api/replays` | 存档列表 |
| GET | `/api/load_replay/<id>` | 加载存档 |
| DELETE | `/api/archive/<name>` | 删除存档 |
| PUT | `/api/archive/<name>` | 重命名存档 |
| GET | `/api/archive/check/<name>` | 检查重名 |

---

## 七、环境与启动

### 7.1 环境要求
- Python 3.9+
- 推荐浏览器：Chrome / Edge / Firefox

### 7.2 启动方式
**Windows**：双击 `启动跳棋.bat`，自动安装依赖并启动，浏览器访问 `http://127.0.0.1:5000`

**macOS / Linux**：
```bash
cd 跳棋
pip install -r requirements.txt
python app.py
```

---

## 八、视频录制分镜脚本（2 分钟内）

以下为推荐的一段 **约 1 分 45 秒** 介绍视频脚本，需控制语速与操作节奏：

| 时间 | 画面 | 旁白要点 |
|------|------|----------|
| 0:00-0:08 | 打开页面，展示整体界面 | 介绍是一款 Web 跳棋，支持人机/人人对战 |
| 0:08-0:25 | 依次点击 6 个主题色块（每个约 2-3 秒） | 展示 6 套皮肤（iOS/极简/卡通 × 深浅），强调棋盘面板联动变色 |
| 0:25-0:38 | 开启 BGM，快速切换 2-3 种风格 | 说明 BGM 与主题解耦，可选多种风格；顺带点一句落子音效 |
| 0:38-1:00 | 新建 PVE 对局，演示走棋（含连跳） | 一句带过三级 AI 难度；演示平移 + 连跳，展示 AI 提示高亮 |
| 1:00-1:12 | 点击 ↩ 悔棋 | 演示悔棋功能 |
| 1:12-1:28 | 点击 🎬 回放 + 展示存档管理 | 演示对局回放逐步复盘；一句带过命名/删除/重命名 |
| 1:28-1:40 | （可选）展示 LLM 点评 | AI 电竞解说风格点评；未配置 Key 则直接跳过 |
| 1:40-1:45 | 结束语 | 总结亮点：6 主题 + 音频 + 三级 AI + 回放存档 |

### 录制小贴士
1. **全程控制在 2 分钟内**：每个环节只演示最关键一步，不深入细节
2. **录 PVE 模式**选择 ★ 随机难度，节奏更可控
3. **展示 6 套主题**时切换停顿约 2 秒即可，无需逐个讲解
4. 若未配置 DeepSeek Key，直接跳过 LLM 点评环节
5. 可先用两倍速剪辑回放/存档环节，省出时间给主题与音效展示

---

## 九、常见问题

| 问题 | 解决方案 |
|------|----------|
| 端口被占用 | 修改 `app.py` 末行 `port=5000` 为其他端口 |
| 听不到音乐 | 需先点击页面任意位置解锁音频（浏览器策略），再开启 BGM |
| 主题没生效 | 浏览器缓存，强制刷新（Ctrl+F5） |
| AI 点评不显示 | 检查是否输入了有效的 DeepSeek API Key |
| 存档丢失 | 存档保存在 `跳棋/replays/` 目录，勿删除 |

---

*文档版本：v3.0（对应 2nd_edition 功能集）*
